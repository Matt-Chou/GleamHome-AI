Login image assets

Status: The example SVG images that were previously created in this folder have been removed per user request.

If you'd like to recreate assets, I can:
- regenerate SVGs with a different style or palette
- export PNG variants instead
- add the chosen assets back into `pubspec.yaml` and run `flutter pub get`

Tell me which option you prefer and any visual references or color palettes, and I'll proceed.
